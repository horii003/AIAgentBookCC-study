---
version: "1.1.0"
last_updated: "2026-04-03"
updated_by: ""
---

# バリデーション方針

> **参照元（システム要件定義資料）:**
> - データ一覧.md、テーブル一覧.md（バリデーション対象データの特定・データ形式）
> - 機能ツール一覧.md（ツールパラメータのバリデーション要件）
> - 業務ルール定義.md（業務ルールに基づくバリデーション）
> - ガードレール要件定義.md（入力ガードレール・データ整合性チェック）
> - 会話フロー一覧.md（バリデーションのタイミング）

> 文書ID：`SYS-VALIDATION-001`
> 文書名：バリデーション方針
> 版数：`v1.0`
> 作成日：YYYY-MM-DD

---

## 1. 目的・適用範囲

### 1.1 目的

### 1.2 適用範囲

### 1.3 本方針で定義すること

### 1.4 本方針で定義しないこと

---

## 2. バリデーションの基本原則

### 2.1 基本方針

### 2.2 バリデーションのタイミング

| タイミング | 対象 | 実装場所 |
|----------|------|---------|
| | | |

### 2.3 バリデーション失敗時の扱い

**ValidationError発生時**:
- 

---

## 3. Pydanticモデルの使用方針

### 3.1 基本原則

### 3.2 Pydanticモデルの定義場所

**定義場所**: 

**命名規則**: 

### 3.3 Pydanticモデルの基本構造

```python
from pydantic import BaseModel, field_validator, Field
from typing import Optional

class ExampleInput(BaseModel):
    pass
```

---

## 4. [データ型1]のバリデーション

### 4.1 基本原則

### 4.2 [データ型1]の定義

**標準形式**: 

**許容値**: 

### 4.3 [データ型1]バリデーションの実装

```python
@field_validator('[フィールド名]')
@classmethod
def validate_[フィールド名](cls, v):
    pass
```

### 4.4 [データ型1]の特殊ルール

---

## 5. [データ型2]のバリデーション

### 5.1 基本原則

### 5.2 [データ型2]の定義

### 5.3 [データ型2]バリデーションの実装

### 5.4 [データ型2]の特殊ルール

---

## 6. カスタムバリデーションルール

### 6.1 列挙型フィールドのバリデーションと正規化

**実装パターン**:
```python
from typing import Literal
from pydantic import field_validator

field_name: Literal["[値1]", "[値2]"]

@field_validator('[フィールド名]')
@classmethod
def normalize_[フィールド名](cls, v: str) -> str:
    mapping = {
        "[別表記1]": "[値1]",
    }
    return mapping.get(v, v)
```

### 6.2 [カスタムルール2]

---

## 7. バリデーションエラーメッセージ

### 7.1 基本原則

### 7.2 エラーメッセージの形式

**基本形式**:
```
{フィールド名}のエラー: {エラー内容}
対処方法: {対処方法}
```

### 7.3 エラーメッセージの実装

```python
from pydantic import ValidationError

try:
    model = ExampleInput(**data)
except ValidationError as e:
    error_message = error_handler.handle_validation_error(e)
```

---

## 8. バリデーションのテスト

### 8.1 テスト観点

### 8.2 テストケースの例

| テストケース | 入力 | 期待結果 |
|------------|------|---------|
| | | |

---

## 9. 設計上の留意点・リスク

### 9.1 [留意点1]

**リスク**: 

**対策**: 

### 9.2 [留意点2]

**リスク**: 

**対策**: 

---

## 10. 未決事項

| ID | 内容 | 対応予定 |
|----|------|---------|
| | | |

---

## 11. 変更履歴

| 日付 | 版 | 変更内容 | 担当 |
|-----|---|---------|------|
| YYYY-MM-DD | v1.0 | 初版作成 | - |

---
